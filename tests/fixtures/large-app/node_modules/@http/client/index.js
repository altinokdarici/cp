import { Headers } from '@http/headers';
import { buildRequest } from './request';
export function get(url) { return buildRequest('GET', url, new Headers()); }
export function post(url, body) { return buildRequest('POST', url, new Headers(), body); }
